// prettier.config.js
module.exports = {
  // Use single quotes instead of double quotes
  singleQuote: true,

  // Indent using 2 spaces
  tabWidth: 2,

  // Use semicolons at the end of statements
  semi: true,

  // Ensure trailing commas are included where possible (e.g., objects, arrays)
  trailingComma: "es5",

  // Enclose objects with parentheses only when needed
  bracketSpacing: true,

  // Use arrow function parentheses when there's only one argument
  arrowParens: "always",

  // Format on save (set up your editor to auto-save or trigger formatting)
  endOfLine: "auto",
};
